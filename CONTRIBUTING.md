# 기여 규칙

## 브랜치와 PR

- 브랜치 이름: `feat/역할-작업명` (예: `feat/onchain-price-collector`), 버그는 `fix/...`
- main 직접 push 금지 — 모든 변경은 PR로만
- PR 제목: `[역할] 한 줄 요약` (예: `[engine] 페그 이탈 정규화 근거 반영`)
- PR 본문에 반드시 포함: 무엇을 / 왜 / 어떻게 테스트했는지
- CI(ruff + 계약 테스트)가 초록색이어야 리뷰 시작 — 단, 초록불이 곧 정답은 아님을 기억할 것

## 코드 규칙

- 순수 함수 원칙: `src/` 안의 함수는 파일·네트워크를 만지지 않음. I/O는 러너/핸들러가 주입
- 스키마(schemas/)는 팀 간 계약 — 필드 추가·변경은 코드보다 먼저 리뷰 미팅에서 합의
- 새 지표(metric)를 추가하려면: 스키마 enum 수정 + 계약 테스트 + 엔진 정규화 근거 문서화가 한 PR에
- 자기가 만든 모듈은 자기가 문서화 (모듈 docstring + 필요 시 docs/)

## AI 코딩 도구

- 설명 못 하는 코드는 제출하지 않음 — 리뷰에서 "이 줄 왜?"에 답할 수 있어야 함
- 스키마·인터페이스 변경은 AI가 아니라 미팅에서 결정
- API 키 등 비밀값을 AI 도구에 입력 금지 (이 저장소에는 애초에 비밀값이 없어야 정상)

## 커밋

- 형식: `타입: 요약` — 타입은 feat / fix / docs / test / refactor
- 예: `feat: USDC 가격 시그널 변환 함수 추가`
